# Audio Archive

The CSV is the master data source. This version adds **Year** as an independent filter alongside Gender.

Filters:
- Search
- Gender
- Year
- Themes

Desktop: left filter sidebar.
Mobile: sidebar becomes a slide-out drawer.

When you revise the CSV/Excel later, send the new file and I can regenerate `data.js` for you, or regenerate the whole site ZIP if you changed structure/functionality too.
